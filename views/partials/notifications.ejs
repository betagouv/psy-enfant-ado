<% errors.forEach((error, index)=> { %>
      <%- include('notification-error', {error, index}) -%>
<% }) %>
<% infos.forEach((info, index)=> { %>
      <%- include('notification-info', {info, index}) -%>
<% }) %>